name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - flutterflow # O la rama donde hagas tus pushes

jobs:
  build_and_deploy_job:
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.4' # Usa la versión de Flutter de tu proyecto
          channel: 'stable'

      - name: Build Flutter web app
        run: |
          flutter config --enable-web
          flutter pub get
          flutter build web --release

      - name: Deploy to Azure Static Web App
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_ASHY_PEBBLE_037E8B010 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "/" 
          output_location: "build/web"
